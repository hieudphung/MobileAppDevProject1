import 'package:flutter/material.dart';

class RecentGoalsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Recent Goals'),
      ),
      body: Center(
        child: Text('Recent Goals Page Content'),
      ),
    );
  }
}
